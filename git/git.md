<!-- //bhai isme git ke kuch commands ke bare me btaya hai
git -v : git version
git init : initialize git in a project
git add . : add all the file to git
git add <file name > : add the particular file to git
git commit -m "Message" : commit the added files
git push origin main : push the commits to the main branch 
git remote add origin <link> : to add/link remote repo 
git remote remove origin : remove the remote repo
git remote -v : path of the remote repo 
git branch : current branch 
git branch -M main : change current branch name to main
git status : current status of the files
git checkout <branch name> : navigate to different branch
git checkout -b <branch name > : create new branch and navigate to it
git branch -d <branch name > : delete branch

merging to branches 
i). 1. git diff <branch name jisse merge krna >
    2. git merge <branch name>

ii). Using pull request : ye git hub se hota hai 
git hub se merge krne ke bad use apne local system pr kaise laye  ? 
fir hum likhenge : git pull origin main 
ye main ko pull krlega  -->
